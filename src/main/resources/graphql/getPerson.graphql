query($ident: ID!){
  hentIdenter(ident: $ident, grupper: AKTORID, historikk: false) {
    identer {
      ident,
      historisk,
      gruppe
    }
  }
  hentPerson(ident: $ident) {
  	navn(historikk: false) {
  	  fornavn
  	  mellomnavn
  	  etternavn
    }
  }
}
